package com.exam.system.config;

import org.springframework.context.annotation.Configuration;

/**
 * Marker configuration for rate limiter settings bound via {@link SecurityProperties}.
 */
@Configuration
public class RateLimiterConfig {
}
